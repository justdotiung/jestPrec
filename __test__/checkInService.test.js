var Conference = require('../callbackPattern');

describe('Conference.checkInService', () => {
    var checkInService,
        checkInRecorder,
        atendee;

        beforeEach(() => {
            checkInRecorder = Conference.
        })
})